
main :: () {
    global_variable -= 1;
    
    a : int = 2 + global_variable + CONSTANT;
    b := 3;
    c := 143 / 8 % 2;
    b = a << c;
    // comment 
    print a;
    print b;
    
    v := 0;
    print inc(v);
}